{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "amc.serviceAccountName" . }}
  labels:
    {{- include "amc.labels" . | nindent 4 }}
{{- end }}
