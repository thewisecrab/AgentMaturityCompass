# Optional environment hints for OpenClaw + AMC baseline
# Copy to .env if your local setup needs explicit values.

# AMC config
AMC_HOME=.amc
AMC_MODE=local

# OpenClaw runtime
OPENCLAW_CONFIG=examples/openclaw/config.yaml
